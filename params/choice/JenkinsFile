pipeline{
    agent any

    parameters{
        choice(choices: ["TEST", "DEV", "QA", "PRE-PRO", "PROD"], description: "Which Environment to deploy it", name: "deployEnv")
    }

    stages{
        stage("Demo"){
            steps{
                echo "Choice is set to: ${params.deployEnv}"
            }
        }
    }
}